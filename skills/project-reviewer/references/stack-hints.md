# Stack Hints

Read only the sections that match inventory stack tags.

## Node / TypeScript

Timeouts on fetch, no unbounded JSON, Zod on public boundaries.

## Python

No ORM in loops, requests always with timeout, no bare except on write paths.

## Infra

No latest tags, healthcheck, memory limits, secrets not in compose committed files.
