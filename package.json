{
  "name": "faceless-production-suite",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "build:contract": "pnpm --filter @faceless/schema build:contract"
  }
}
